# Backend (Python sidecar) dependencies for OST Tracker.
# The frontend is the SwiftUI app in OSTTracker/; these are what the
# FastAPI sidecar and the ost_tracker domain layer need at runtime.

# Sidecar API (backend/api.py), consumed by the SwiftUI frontend.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # /import/portable (multipart upload)

# Domain layer: cover-art HTTP + image caching.
httpx>=0.27
Pillow>=10

# In-app playback stream resolution. NOT build-once-forget: YouTube changes
# routinely break extraction until yt-dlp ships a fix, so update it
# periodically (pip install -U yt-dlp). Playback degrades to opening the
# link in the browser whenever resolution fails or yt-dlp is missing.
yt-dlp>=2025.1.1

# Dev / test (not required to run a packaged .app):
#   pytest, pytest-cov

# Legacy: the retired PySide6 UI (now under legacy/python-frontend/) additionally
# needed  PySide6>=6.6  and  qtawesome>=1.3 . Install those manually only if you
# want to run the archived desktop UI — they are no longer part of the app.
